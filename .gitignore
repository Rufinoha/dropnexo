# Segredos e ambiente local
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/

# Uploads de tenant (dados locais)
upload/**
!upload/.gitkeep

# Certificados
*.p12

# IDE / ferramentas locais
.cursor/
.vscode/
*.log
# Legado local — manter no disco, nunca versionar
old/
OLD/
!OLD/HUBSUPPORT/.git/

# SQL manual (migrações locais — não versionar)
*.sql
__doc/sql/

# Documentação interna (__doc/) — demais arquivos podem ser versionados
